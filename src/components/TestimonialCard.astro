---
import OptimizedImage from './OptimizedImage.astro';

interface Props {
  name: string;
  role: string;
  quote: string;
  image?: string;
}

const { name, role, quote, image } = Astro.props;
---

<div class="bg-white rounded-2xl p-6 shadow-sm border border-gray-100 transition-all duration-300 hover:shadow-lg hover:border-[var(--color-teal)]/20 hover:-translate-y-1 group">
  <div class="flex items-center gap-4 mb-4">
    {image ? (
      <OptimizedImage src={image} alt={name} class="w-12 h-12 rounded-full object-cover ring-2 ring-transparent group-hover:ring-[var(--color-teal)]/30 transition-all" />
    ) : (
      <div class="w-12 h-12 rounded-full bg-[var(--color-cream)] flex items-center justify-center ring-2 ring-transparent group-hover:ring-[var(--color-teal)]/30 transition-all">
        <span class="text-lg font-semibold text-[var(--color-primary)]">
          {name.charAt(0)}
        </span>
      </div>
    )}
    <div>
      <h3 class="font-semibold text-gray-900 text-base group-hover:text-[var(--color-teal)] transition-colors">{name}</h3>
      <p class="text-sm text-gray-500">{role}</p>
    </div>
  </div>
  <blockquote class="text-gray-600 italic leading-relaxed">
    "{quote}"
  </blockquote>
</div>
